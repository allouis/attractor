Understand the bug reported in issue $context.identifier — "$context.title" — in $context.repo.

The issue is at $context.url. The repo is checked out in your working directory.

1. Read the issue in full: the symptom, any steps to reproduce, expected
   vs actual behaviour, and any stack traces or logs.
2. Read the code paths the report points at. Form a concrete hypothesis
   for the root cause — not just the symptom.

Write no code yet. Summarise the bug, your root-cause hypothesis, and how
you will reproduce it as an automated, failing test.

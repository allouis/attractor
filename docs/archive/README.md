# Archived specs

Design docs for subsystems deleted in the 2026-08-13 strip (see
`../local-first-plan.md`, postscript): the serve daemon + web UI, VM
launchers + nix image, items intake (GitHub/Linear) + router, phone-home
reporting, cron automations, and their build logs. Kept for history;
nothing in here describes the current tool.
